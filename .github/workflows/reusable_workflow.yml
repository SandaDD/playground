on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string
      labels:
        required: true
        type: string

  
jobs:
  run-on-extracted-labels:
    runs-on: ubuntu-latest
    outputs:
      array:  ${{ steps.extractLabels.outputs.labels }}
    env:
      JSON_DOC: ${{toJson(inputs)}}
    steps:
      - name: Welcome message
        run: |
          printf '%s\n' "$JSON_DOC" > foo.json
          cat foo.json

      - name: extract labels
        id: extractLabels
        run: |
          echo "labels=${{(inputs.labels)}}" >> $GITHUB_OUTPUT
          echo "$JSON_DOC.labels" 
          echo "${{fromJson(toJson(inputs.labels))}}" 
          echo "$GITHUB_OUTPUT"

  

        
